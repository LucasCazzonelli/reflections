What happens when you initialize a repository? You bind a repository to a specific directory. Why do you need to do it? To let git know about the directory?. 

How is the staging area different from the working directory and the repository? It is an intermediate between the working directory and the repository. 
What value do you think it offers? It allows you to commit changes to multiple files at once. 

How can you use the staging area to make sure you have one commit per logical
change? It allows you to add all logically connected changes to it and commit them all at once. 

What are some situations when branches would be helpful in keeping your history
 organized? It helps if you want to experiment with changes that might not make it into the final version of the program. How would branches help? They help by creating a seperate branch of commits which enable you to expirement with the sourcecode while leaving the main branch unaffected. 

How do the diagrams help you visualize the branch structure?

What is the result of merging two branches together? Why do we represent it in
the diagram the way we do?

What are the pros and cons of Git's automatic merging vs. always doing merges 
manually?
